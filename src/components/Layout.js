    import React from 'react';

    export default function Layout({ children })
    {
        return (
            <div style={{ paddingTop: '2rem' }}>
                {/* ...existing code... */}
                {children}
                {/* ...existing code... */}
            </div>
        );
    }